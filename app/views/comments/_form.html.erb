<%= form_for([@post, @post.comments.build]) do |form| %>

  <div class="field">
    <%= form.text_area :body, id: :comment_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
